{
  "name": "interview",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "init": "rimraf db.sqlite && tsx init/db.ts",
    "test": "npm run init  && tsx --test **/*.test.ts",
    "test:watch": "npm run init  && tsx --test --watch **/*.test.ts"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "fast-xml-parser": "^4.5.0",
    "sqlite": "^5.1.1",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "rimraf": "^6.0.1",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
